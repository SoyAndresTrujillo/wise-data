# Generate a new secret with: `openssl rand -base64 32`
API_KEY=

# Generate a new secret with: `openssl rand -base64 32`
JWT_SECRET=

## Use with Docker - DB
DATABASE_URL=postgresql://localhost/postgres?user=root&password=root
DATABASE_PORT=5432
DB_USER=user
DB_PASSWORD=password
DB_NAME=name
DB_HOST=host
